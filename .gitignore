# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# ML artifacts / data (keep the repo light) — anchored to repo root so they don't
# match source package dirs like src/corseacare/data/
/runs/
/weights/
*.pt
*.onnx
*.engine
/datasets/
/data/*
# ...except the published source images (CC0, tracked via Git LFS — see .gitattributes):
!/data/corseacare/
/data/corseacare/*
!/data/corseacare/*.jpg
!/data/corseacare/*.JPG
/images/
*.zip

# OS / editor
.DS_Store
.vscode/
.idea/

# scratch
.repos_raw.json

# working-note specs/plans (kept local)
docs/superpowers/

# CLI outputs (root)
/counts.csv
/report.json
/report_by_sample.csv
/overlay.png
