# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# ML artifacts / data (keep the repo light) — anchored to repo root so they don't
# match source package dirs like src/corseacare/data/
/runs/
/weights/
*.pt
*.onnx
*.engine
/datasets/
/data/
/images/
*.zip

# OS / editor
.DS_Store
.vscode/
.idea/

# scratch
.repos_raw.json

# working-note specs/plans (kept local)
docs/superpowers/

# private per-sample manifest (generate your own with make_manifest.py; see samples.csv.example)
samples.csv

# CLI outputs (root)
/counts.csv
/report.json
/report_by_sample.csv
/overlay.png
